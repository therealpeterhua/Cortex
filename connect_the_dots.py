# Require everything here
# What's Python's require relative syntax??
# from .lib import neural_net --> no worky :(

# When you execute a file directly, it doesn't have it's usual name, but has "__main__" as its name instead. So relative imports don't work. You can, as Igancio suggested, execute it using the -m option.
